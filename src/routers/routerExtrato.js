const {Router} = require('express');
const router = Router();


// Adicionar algo ao o extrato
router.post('/',);

// Chamar o extrato
router.get('/',);

// Chamar o extrato unico
router.get('/:id',);

// Deletar o extrato
router.delete('/',);
