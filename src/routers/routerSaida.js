const {Router} = require('express');
const router = Router();

// Create
router.post('/', )

// GetAll
router.get('/', )

// GetOne
router.get('/:id', )

// Update
router.put('/:id', )

router.delete('/:id', )

module.exports = router;